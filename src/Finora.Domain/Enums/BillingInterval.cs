namespace Finora.Domain.Enums;

/// <summary>Billing cadence for a paid plan. Annual is priced with 2 months free by default.</summary>
public enum BillingInterval
{
    Monthly = 0,
    Annual = 1
}
